# Grafana 数据源自动配置
# 启动后 Prometheus 数据源会自动注册到 Grafana，无需手动添加。
apiVersion: 1

datasources:
  - name: Gaia-Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    uid: gaia-prometheus
